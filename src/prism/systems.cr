require "./systems/crash_adapter"
require "./systems/**"

# A collection of pre-build systems.
# A `Crash::System` operates over a subset of `Prism::Entity`s at scheduled intervals.
module Prism::Systems
end
